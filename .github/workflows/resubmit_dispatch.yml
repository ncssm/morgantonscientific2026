name: Resubmit all 2026 articles
on: workflow_dispatch
permissions:
  contents: write
jobs:
  resubmit-papers:
    uses: curvenote/actions/.github/workflows/submit.yml@v1
    with:
      id-pattern-regex: '^morganton-2026-(?:[a-zA-Z0-9-_]{2,15})$'
      venue: ncssm-mor
      collection: '2026'
      kind: original
      path: papers/*
      include-unchanged: true
    secrets:
      CURVENOTE: ${{ secrets.CURVENOTE_TOKEN }}
      GITHUB: ${{ secrets.GITHUB_TOKEN }}
  resubmit-essays:
    uses: curvenote/actions/.github/workflows/submit.yml@v1
    with:
      id-pattern-regex: '^morganton-2026-(?:[a-zA-Z0-9-_]{2,15})$'
      venue: ncssm-mor
      collection: '2026'
      kind: essay
      path: essays/*
      include-unchanged: true
    secrets:
      CURVENOTE: ${{ secrets.CURVENOTE_TOKEN }}
      GITHUB: ${{ secrets.GITHUB_TOKEN }}
