name: Publish all 2026 articles
on: workflow_dispatch
permissions:
  contents: write
jobs:
  publish-papers:
    uses: curvenote/actions/.github/workflows/submit.yml@v1
    with:
      id-pattern-regex: '^morganton-2026-(?:[a-zA-Z0-9-_]{2,15})$'
      venue: ncssm-mor
      collection: '2026'
      kind: original
      path: papers/*
      publish: true
      include-unchanged: true
      debug: true
      comment-title: 'Curvenote Publish — Papers'
    secrets:
      CURVENOTE: ${{ secrets.CURVENOTE_TOKEN }}
      GITHUB: ${{ secrets.GITHUB_TOKEN }}
  publish-essays:
    uses: curvenote/actions/.github/workflows/submit.yml@v1
    with:
      id-pattern-regex: '^morganton-2026-(?:[a-zA-Z0-9-_]{2,15})$'
      venue: ncssm-mor
      collection: '2026'
      kind: essay
      path: essays/*
      publish: true
      include-unchanged: true
      debug: true
      comment-title: 'Curvenote Publish — Essays'
    secrets:
      CURVENOTE: ${{ secrets.CURVENOTE_TOKEN }}
      GITHUB: ${{ secrets.GITHUB_TOKEN }}
